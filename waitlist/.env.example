# Use either VITE_* (waitlist) or EXPO_PUBLIC_* (shared with the Expo app in repo root .env)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# On Vercel: Project Settings → Environment Variables → add both vars, then redeploy
