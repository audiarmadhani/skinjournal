import { View } from 'react-native';

/** Placeholder tab — the + button opens /camera/capture directly. */
export default function CameraTab() {
  return <View className="flex-1 bg-background" />;
}
